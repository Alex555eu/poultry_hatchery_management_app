import { TaskUtilsPipe } from './task-utils.pipe';

describe('TaskUtilsPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskUtilsPipe();
    expect(pipe).toBeTruthy();
  });
});
