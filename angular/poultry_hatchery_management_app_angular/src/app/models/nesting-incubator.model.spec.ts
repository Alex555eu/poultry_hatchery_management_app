import { NestingIncubator } from './nesting-incubator.model';

describe('NestingIncubator', () => {
  it('should create an instance', () => {
    expect(new NestingIncubator()).toBeTruthy();
  });
});
